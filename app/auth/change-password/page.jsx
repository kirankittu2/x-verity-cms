import ChangePassword from "@/app/ui/change-password";
import { Suspense } from "react";

export default function ChangePasswordMain() {
  return (
    <Suspense>
      <ChangePassword />
    </Suspense>
  );
}
